plugin:
    open: true
    name: 插件
    description: 核心功能，用于所有功能的集成
    config:
        - a.txt
        - b.txt
    sql:
        - database: /usr/local/db
          table: logrsv

plugin:
    open: true
    name: 插件
    description: 核心功能，用于所有功能的集成
    config:
        - a.txt
        - b.txt
    sql:
        - database: /usr/local/db
          table: 
        - database: /usr/local/db
          table: logsrv
    